export default function (context) {
  if (!process.server) {
    // let params = `ltime=${new Date().getTime() / 1000}&url=${document.URL}`
    // let dom = document.createElement('img')
    // dom.src = '/statistics/pv?' + params
  }
}
